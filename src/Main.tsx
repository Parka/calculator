import Keyboard from "./components/Keyboard";

const Main = () => (
  <main>
    <h2 className="text-3xl text-blue-700">testing</h2>
    <Keyboard onClick={console.log}/>
  </main>
);

export default Main;
